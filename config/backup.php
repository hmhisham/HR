<?php

return [
    /*
    |--------------------------------------------------------------------------
    | إعدادات النسخ الاحتياطي
    |--------------------------------------------------------------------------
    |
    | هذا الملف يحتوي على إعدادات النسخ الاحتياطي للنظام
    |
    */

    // مسار حفظ النسخ الاحتياطية
    'backup_path' => env('BACKUP_PATH', 'D:\Backups\HR'),

    // كلمة المرور الافتراضية للنسخ الاحتياطي
    'default_password' => env('BACKUP_PASSWORD', 'yas@1989'),

    // البريد الإلكتروني الذي سيتم إرسال النسخ الاحتياطية إليه
    'email' => env('BACKUP_EMAIL', 'yasksalim@gmail.com'),

    // العناصر التي سيتم نسخها احتياطيًا بشكل افتراضي
    'default_items' => ['files', 'database'],

    // إعدادات جدولة النسخ الاحتياطي التلقائي
    'schedule' => [
        'time' => env('BACKUP_SCHEDULE_TIME', '00:00'),
        'frequency' => env('BACKUP_SCHEDULE_FREQUENCY', 'daily'),
    ],

    // إعدادات قاعدة البيانات للنسخ الاحتياطي
    'database' => [
        // مسار أمر mysqldump (اتركه فارغًا لاستخدام المسار الافتراضي)
        'mysqldump_path' => env('MYSQLDUMP_PATH', ''),
    ],
];
